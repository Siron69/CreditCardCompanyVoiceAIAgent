"use strict";
/**
 * LangChain type exports for the Wonderful runtime.
 *
 * NOTE: This file uses actual @langchain package imports.
 * The UI build process copies the .d.ts files and rewrites imports to relative paths.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.LangChainCore = exports.LangChainAnthropic = exports.LangChainGoogleGenAI = exports.LangChainOpenAI = void 0;
const LangChainOpenAIImport = __importStar(require("@langchain/openai"));
const LangChainAnthropicImport = __importStar(require("@langchain/anthropic"));
const LangChainGoogleGenerativeAIImport = __importStar(require("@langchain/google-genai"));
const vertex_sdk_1 = require("@anthropic-ai/vertex-sdk");
// @langchain/core has an empty root export - must import from subpaths
const LangChainCoreMessagesImport = __importStar(require("@langchain/core/messages"));
const LangChainCorePromptsImport = __importStar(require("@langchain/core/prompts"));
const LangChainCoreOutputParsersImport = __importStar(require("@langchain/core/output_parsers"));
const LangChainCoreRunnablesImport = __importStar(require("@langchain/core/runnables"));
exports.LangChainOpenAI = LangChainOpenAIImport;
exports.LangChainGoogleGenAI = LangChainGoogleGenerativeAIImport;
// Placeholder values for Vertex project/region. The runner proxy replaces
// these in the outgoing URL with the real values from the controller config,
// so no environment variables need to be injected into the runner.
const VERTEX_PROJECT_PLACEHOLDER = "__vertex_project__";
const VERTEX_REGION_PLACEHOLDER = "__vertex_region__";
/**
 * ChatAnthropic that routes all requests through Google Vertex AI using
 * the official @anthropic-ai/vertex-sdk.
 *
 * Auth and URL rewriting are handled by the runner proxy: it replaces the
 * placeholder project/region in the URL with real values and injects a
 * bearer token, so we pass dummy values here.
 */
class VertexChatAnthropic extends LangChainAnthropicImport.ChatAnthropic {
    constructor(fields) {
        const config = fields ?? {};
        super({
            ...config,
            createClient: () => new vertex_sdk_1.AnthropicVertex({
                projectId: VERTEX_PROJECT_PLACEHOLDER,
                region: VERTEX_REGION_PLACEHOLDER,
                accessToken: "proxy-managed",
                dangerouslyAllowBrowser: true,
            }),
        });
    }
}
exports.LangChainAnthropic = {
    ...LangChainAnthropicImport,
    ChatAnthropic: VertexChatAnthropic,
};
exports.LangChainCore = {
    ...LangChainCoreMessagesImport,
    ...LangChainCorePromptsImport,
    ...LangChainCoreOutputParsersImport,
    ...LangChainCoreRunnablesImport,
};
//# sourceMappingURL=langchain.js.map