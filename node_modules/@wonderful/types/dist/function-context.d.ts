import type { BaseContextLibrary, ContextMetadata } from "./context";
export interface FunctionContext extends BaseContextLibrary {
    /**
     * The input data passed to this function from query and body params.
     */
    data: Record<string, any>;
    /**
     * Session metadata. Use `metadata.user` to identify the calling tenant
     * user (set for API Function calls made with a session or user-scoped
     * API key; undefined for cron, public app session-token, admin-API-key
     * and external-entity callers). `metadata.tenantId` is always set on
     * function/cron-function invocations.
     */
    metadata: ContextMetadata;
}
//# sourceMappingURL=function-context.d.ts.map