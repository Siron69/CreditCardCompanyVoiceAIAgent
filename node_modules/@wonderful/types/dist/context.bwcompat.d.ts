/**
 * Compile-time backwards-compatibility checks for the `Context` type.
 *
 * No runtime test framework is wired up in this package, so these assertions
 * are TypeScript-level: any change that breaks the documented surface will
 * fail `tsc --noEmit` and surface during `pnpm run build`.
 *
 * Per @maxim-l-wonderful's review on PR #6914 — adding `storage` to `Context`
 * is additive; older tools that never reference `ctx.storage` must keep
 * type-checking, and the new methods must keep their documented shape.
 */
export {};
//# sourceMappingURL=context.bwcompat.d.ts.map