"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.customFetch = customFetch;
exports.createBaseContextLibrary = createBaseContextLibrary;
exports.createToolContextLibrary = createToolContextLibrary;
// @ts-ignore
const telephonyFunctions = __importStar(require("wonderful:telephony/functions"));
// @ts-ignore
const agentFunctions = __importStar(require("wonderful:agent/functions"));
// @ts-ignore
const kvFunctions = __importStar(require("wonderful:kv/functions"));
// @ts-ignore
const secretsFunctions = __importStar(require("wonderful:secrets/functions"));
// @ts-ignore
const globalsFunctions = __importStar(require("wonderful:globals/functions"));
// @ts-ignore
const sessionFunctions = __importStar(require("wonderful:session/functions"));
// @ts-ignore
const toolsFunctions = __importStar(require("wonderful:tools/functions"));
// @ts-ignore
const attachmentsFunctions = __importStar(require("wonderful:attachments/functions"));
// @ts-ignore
const outboundFunctions = __importStar(require("wonderful:outbound/functions"));
// @ts-ignore
const campaignFunctions = __importStar(require("wonderful:campaign/functions"));
// @ts-ignore
const a2aFunctions = __importStar(require("wonderful:a2a/functions"));
// @ts-ignore
const tablesFunctions = __importStar(require("wonderful:tables/functions"));
// @ts-ignore
const functionsFunctions = __importStar(require("wonderful:functions/functions"));
// @ts-ignore
const transcriberFunctions = __importStar(require("wonderful:transcriber/functions"));
// @ts-ignore
const customStorageFunctions = __importStar(require("wonderful:custom-storage/functions"));
// @ts-ignore
const functions_1 = require("wonderful:date/functions");
function createKv() {
    const serializeKVValue = (value) => {
        if (value === undefined) {
            console.error("[warning] kv serialize operation received undefined; casting to null");
            return "null";
        }
        return JSON.stringify(value);
    };
    return {
        /**
         * Retrieves the value associated with the given key.
         * Throws an error if the key does not exist.
         */
        get: (key) => {
            const value = kvFunctions.get(key);
            try {
                return value ? JSON.parse(value) : null;
            }
            catch (e) {
                console.error("Failed to parse KV value:", value);
                throw e;
            }
        },
        /**
         * Sets the value associated with the given key.
         *
         * The key must be a string and the value can be any JSON-serializable type.
         * Top-level `undefined` is stored as `null` and emits a warning in tool logs.
         *
         * If the key already exists, the value will be overwritten.
         */
        set: (key, value) => {
            kvFunctions.set(key, serializeKVValue(value));
        },
        /**
         * Sets the value only if the key does not already exist.
         *
         * Returns true if the value was set, false if the key already existed.
         * Top-level `undefined` is stored as `null` and emits a warning in tool logs.
         */
        setIfNotExist: (key, value) => {
            return kvFunctions.setNx(key, serializeKVValue(value));
        },
        exists: (key) => {
            return kvFunctions.exists(key);
        },
        delete: (key) => {
            kvFunctions.delete(key);
        },
        /**
         * Deletes the key only if the existing value matches the expected value.
         *
         * Top-level `undefined` is stored as `null` and emits a warning in tool logs.
         */
        deleteIfEqual: (key, expectedValue) => {
            return kvFunctions.deleteIfEqual(key, serializeKVValue(expectedValue));
        }
    };
}
function createSecrets() {
    return {
        get: (name) => {
            const value = secretsFunctions.getSecret(name);
            try {
                if (!value)
                    return null;
                const parsed = JSON.parse(value);
                return unwrapOtherSecret(parsed);
            }
            catch (e) {
                console.error("Failed to parse secret value:", value);
                throw e;
            }
        }
    };
}
// "Other" type secrets are stored as {"value":"..."}.  Unwrap to return the
// plain string so tool authors don't need to reach into the wrapper object.
function unwrapOtherSecret(parsed) {
    if (parsed &&
        typeof parsed === "object" &&
        !Array.isArray(parsed) &&
        Object.keys(parsed).length === 1 &&
        "value" in parsed &&
        typeof parsed.value === "string") {
        return parsed.value;
    }
    return parsed;
}
function createGlobals() {
    return {
        get: (name) => {
            const value = globalsFunctions.getGlobal(name);
            try {
                return value ? JSON.parse(value) : null;
            }
            catch (e) {
                console.error("Failed to parse global value:", value);
                throw e;
            }
        }
    };
}
function createTelephony() {
    return {
        sendSms: (destination, message) => {
            telephonyFunctions.sendExternalMessage("sms", destination, "", message, "", []);
        },
        forward: (destination) => {
            telephonyFunctions.forwardSession(destination);
        },
        end: (shouldWaitForPendingMessages = true) => {
            telephonyFunctions.endSession(shouldWaitForPendingMessages);
        },
        /**
         * Sends DTMF tones to the active call via the Twilio API.
         * WARNING: This will terminate the media stream and end the conversation on our side.
         * The call continues on Twilio until the DTMF playback completes.
         * @param digits - The DTMF digits to send (0-9, *, #)
         */
        sendDtmf: (digits) => {
            telephonyFunctions.sendDtmf(digits);
        },
    };
}
function createAgent() {
    return {
        announce: (message, params) => {
            agentFunctions.announce(message, params);
        },
        forceAnnounce: (message, params) => {
            agentFunctions.forceAnnounce(message, params);
        },
        sendSystemMessage: (message) => {
            agentFunctions.systemMessage(message);
        },
        declareGender: (gender) => {
            agentFunctions.declareGender(gender);
        },
        getGender: () => {
            return agentFunctions.getGender();
        },
        correctTranscription: (transcription) => {
            return agentFunctions.correctTranscription(transcription);
        },
        changeSpeed: (speed) => {
            agentFunctions.changeSpeedLevel(speed);
        },
        switchSkill: (skillName, params) => {
            agentFunctions.switchSkill(skillName, params);
        },
        setEndOfTurnDelay: (delayMs) => {
            agentFunctions.setEndOfTurnDelay(delayMs);
        },
        clearEndOfTurnDelay: () => {
            agentFunctions.clearEndOfTurnDelay();
        },
    };
}
function createEmail() {
    return {
        send: (destination, title, messageOrOptions, options) => {
            let message = "";
            let html = "";
            let attachments = [];
            if (typeof messageOrOptions === "string") {
                message = messageOrOptions;
                attachments = options?.attachments ?? [];
            }
            else {
                if (messageOrOptions.text) {
                    message = messageOrOptions.text;
                }
                html = messageOrOptions.html ?? "";
                attachments = messageOrOptions.attachments ?? [];
            }
            telephonyFunctions.sendExternalMessage("email", destination, title, message, html, attachments);
        },
    };
}
function createSession() {
    const mapResponseGateState = (state) => ({
        enabled: state.enabled,
        state: state.state,
        pendingTurns: state.pendingTurns,
        generationReserved: state.generationReserved,
        closesAfterNextResponse: state.closesAfterNextResponse,
        lastReason: state.lastReason || undefined,
        lastSource: state.lastSource || undefined,
        lastBlockedReason: state.lastBlockedReason || undefined,
    });
    return {
        getTranscription: () => {
            const rawTranscription = sessionFunctions.getTranscription();
            const toMoment = (value) => {
                return value != null ? functions_1.Moment.fromTimestamp(value) : undefined;
            };
            const toNumber = (value) => {
                return value != null ? Number(value) : undefined;
            };
            return rawTranscription.map(item => ({
                id: item.id,
                speaker: item.speaker,
                text: item.text,
                createdAt: functions_1.Moment.fromTimestamp(item.createdAt),
                startTime: toMoment(item.startTime),
                endTime: toMoment(item.endTime),
                interruptedAtCharIndex: toNumber(item.interruptedAtCharIndex),
                confidence: typeof item.confidence === "number" ? item.confidence : undefined,
                toolDetails: item.toolDetails ? {
                    functionName: item.toolDetails.functionName,
                    description: item.toolDetails.description,
                    output: item.toolDetails.output,
                    params: item.toolDetails.paramsJson ? JSON.parse(item.toolDetails.paramsJson) : undefined,
                    isError: item.toolDetails.isError,
                    durationMs: typeof item.toolDetails.durationMs === "number" ? item.toolDetails.durationMs : undefined,
                    callSource: item.toolDetails.callSource ?? undefined,
                    triggerType: item.toolDetails.triggerType ?? undefined,
                } : undefined,
            }));
        },
        forward: (destination) => {
            sessionFunctions.forward(destination);
        },
        updateTimeoutSettings: async (patch) => {
            const toBigInt = (v) => v != null ? BigInt(v) : undefined;
            const mapStep = (step) => step ? { ...step, durationSeconds: toBigInt(step.durationSeconds) } : undefined;
            await sessionFunctions.updateTimeoutSettings({
                conversationTimeLimit: patch.conversationTimeLimit
                    ? { ...patch.conversationTimeLimit, durationSeconds: toBigInt(patch.conversationTimeLimit.durationSeconds) }
                    : undefined,
                userInactivity: patch.userInactivity
                    ? {
                        checkIn: mapStep(patch.userInactivity.checkIn),
                        disconnectWarn: mapStep(patch.userInactivity.disconnectWarn),
                        endConversation: mapStep(patch.userInactivity.endConversation),
                    }
                    : undefined,
            });
        },
        resetTimeoutSettings: async () => {
            await sessionFunctions.resetTimeoutSettings();
        },
        responseGate: {
            /**
             * Returns the current response gate state. The `enabled` field is read-only:
             * enabling response gating is controlled only by agent settings.
             */
            get: () => {
                return mapResponseGateState(sessionFunctions.getResponseGate());
            },
            /**
             * Closes the response gate so future user turns are recorded but do not
             * trigger agent speech. Throws if response gating is not enabled for this session.
             */
            close: (options) => {
                return mapResponseGateState(sessionFunctions.closeResponseGate(options?.reason, options?.interruptCurrent ?? false));
            },
            /**
             * Opens the response gate for future turns. When `generateIfPending` is true,
             * one pending gated turn may be answered if no response is active.
             */
            open: (options) => {
                return mapResponseGateState(sessionFunctions.openResponseGate(options?.reason, options?.generateIfPending ?? false));
            },
            /**
             * Allows exactly the next response, then closes the gate again after that
             * response completes. Throws if response gating is not enabled for this session.
             */
            allowNextResponse: (options) => {
                return mapResponseGateState(sessionFunctions.allowNextResponse(options?.reason, options?.generateIfPending ?? false));
            },
        },
    };
}
function createTranscriber() {
    return {
        reconnect: () => {
            transcriberFunctions.reconnect();
        },
    };
}
function createTools() {
    return {
        call: (name, parameters) => {
            const result = JSON.parse(toolsFunctions.call(name, JSON.stringify(parameters)));
            const parsed = parseToolCallResult(name, result);
            return Promise.resolve(parsed);
        },
        callRag: (name, query) => {
            const result = JSON.parse(toolsFunctions.call(name, JSON.stringify({ query })));
            const answer = parseToolCallResult(name, result);
            if (typeof answer === "string") {
                return Promise.resolve(answer);
            }
            return Promise.resolve(null);
        }
    };
}
function createAttachments() {
    return {
        /**
         * Lists all files attached to this communication.
         * Returns an array of file metadata including id, name, mimeType, and size.
         */
        listFiles: () => {
            const files = attachmentsFunctions.listFiles();
            return Promise.resolve(files.map(f => ({
                id: f.id,
                name: f.name,
                mimeType: f.mimeType,
                size: Number(f.size),
            })));
        },
        /**
         * Downloads a file by its ID and returns the file contents as a Uint8Array.
         * @param id - The unique identifier of the file to download
         */
        getFile: (id) => {
            return Promise.resolve(new Uint8Array(attachmentsFunctions.getFile(id)));
        },
        /**
         * Uploads a new file to this communication's attachment folder.
         * @param name - The name of the file (including extension)
         * @param content - The file contents as a Uint8Array
         * @returns The metadata of the uploaded file
         */
        uploadFile: (name, content) => {
            const result = attachmentsFunctions.uploadFile(name, content);
            return Promise.resolve({
                id: result.id,
                name: result.name,
                mimeType: result.mimeType,
                size: Number(result.size),
            });
        },
    };
}
function createOutbound() {
    return {
        /**
         * Send a message through any connected provider.
         * @param options.provider - The provider name (e.g. "meta", "gmail", "slack")
         * @param options.destination - The recipient (phone number, email, channel ID)
         * @param options.message - Text message content
         * @param options.params - Provider-specific parameters as key-value pairs
         * @param options.connectionName - Optional connection name to route through
         * @returns Response text (e.g. rendered template text for WhatsApp)
         */
        send: (options) => {
            return outboundFunctions.sendOutbound(options.provider, options.connectionName ?? undefined, options.destination, options.message, Object.entries(options.params ?? {}), []);
        },
        whatsapp: {
            /**
             * Send a WhatsApp template message.
             * @param options.destination - Recipient phone number (E.164 format, e.g. "+972546230567")
             * @param options.templateName - Template name from Meta Business Manager
             * @param options.languageCode - Template language code (e.g. "en_US")
             * @param options.templateParams - Ordered parameters for template placeholders
             * @param options.connectionName - Optional connection name to route through
             * @returns The rendered template text
             */
            sendTemplate: (options) => {
                return outboundFunctions.sendOutbound("meta", options.connectionName ?? undefined, options.destination, "", [
                    ["template_name", options.templateName],
                    ["language_code", options.languageCode],
                    ["template_params", JSON.stringify(options.templateParams ?? [])],
                ], []);
            },
        },
        email: {
            /**
             * Send an email through a specific email provider.
             * @param options.provider - The email provider ("gmail" or "outlook")
             * @param options.destination - Recipient email address
             * @param options.subject - Email subject line
             * @param options.text - Plain text body
             * @param options.html - HTML body
             * @param options.attachments - Attachment filenames
             * @param options.connectionName - Optional connection name to route through
             */
            send: (options) => {
                const params = [["email.subject", options.subject]];
                if (options.html)
                    params.push(["email.html_body", options.html]);
                return outboundFunctions.sendOutbound(options.provider, options.connectionName ?? undefined, options.destination, options.text ?? "", params, options.attachments ?? []);
            },
        },
        slack: {
            /**
             * Send a Slack message to a channel or user.
             * @param options.channelId - Slack channel or user ID
             * @param options.message - Message text
             * @param options.threadTs - Optional thread timestamp
             * @param options.attachments - Optional attachments
             * @param options.connectionName - Optional connection name to route through
             */
            send: (options) => {
                const params = [];
                if (options.threadTs)
                    params.push(["chat.thread_id", options.threadTs]);
                return outboundFunctions.sendOutbound("slack", options.connectionName ?? undefined, options.channelId, options.message, params, options.attachments ?? []);
            },
        },
        teams: {
            /**
             * Send a Teams message to a conversation.
             * @param options.conversationId - Teams conversation ID
             * @param options.message - Message text
             * @param options.replyToId - Optional reply-to thread ID
             * @param options.connectionName - Optional connection name to route through
             */
            send: (options) => {
                const params = [];
                if (options.replyToId)
                    params.push(["chat.thread_id", options.replyToId]);
                return outboundFunctions.sendOutbound("teams", options.connectionName ?? undefined, options.conversationId, options.message, params, []);
            },
        },
    };
}
function createCampaign() {
    return {
        callback: (scheduledAt, reason) => {
            campaignFunctions.scheduleCallback(scheduledAt, reason ?? "");
        },
        cancelCallback: () => {
            campaignFunctions.cancelCallback();
        },
        addContact: (phoneNumber) => {
            campaignFunctions.addContact(phoneNumber);
        },
        removeContact: (phoneNumber) => {
            campaignFunctions.removeContact(phoneNumber);
        },
        getConsumerInfo: () => {
            const info = campaignFunctions.getConsumerInfo();
            return {
                firstName: info.firstName ?? null,
                lastName: info.lastName ?? null,
                status: info.status,
                isCallback: info.isCallback,
                contacts: info.contacts.map((c) => ({
                    phoneNumber: c.phoneNumber,
                    status: c.status,
                })),
                previousCalls: info.previousCalls.map((c) => ({
                    attemptNumber: c.attemptNumber,
                    outcome: c.outcome ?? null,
                    tags: c.tags ?? [],
                    metadata: c.metadata ? JSON.parse(c.metadata) : {},
                })),
            };
        },
    };
}
function createA2A() {
    return {
        /**
         * Send a message to a named A2A agent and receive its text response.
         * Only available for tools running with live agent and communication context.
         * This is not available in code editor Run, execute-local flow, standalone functions, or cron functions.
         * The agent name must match an A2A connection attached to the current agent.
         * Sessions are managed automatically per communication.
         * @param agentName - The name of the A2A agent to call
         * @param message - The message to send to the agent
         * @returns The agent's text response
         */
        send: (agentName, message) => {
            return a2aFunctions.send(agentName, message);
        },
    };
}
function createWonderfulFunctions() {
    const schedule = (options) => {
        let runAtISO;
        if (options.runAt instanceof Date) {
            if (isNaN(options.runAt.getTime())) {
                throw new Error("runAt is an invalid Date");
            }
            runAtISO = options.runAt.toISOString();
        }
        else if (typeof options.runAt === "number") {
            const d = new Date(options.runAt);
            if (isNaN(d.getTime())) {
                throw new Error("runAt is an invalid timestamp");
            }
            runAtISO = d.toISOString();
        }
        else {
            if (isNaN(new Date(options.runAt).getTime())) {
                throw new Error("runAt is not a valid date string");
            }
            runAtISO = new Date(options.runAt).toISOString();
        }
        const result = functionsFunctions.schedule(options.slug, runAtISO, JSON.stringify(options.params ?? {}), options.maxRetries ?? 3);
        return { scheduledRunId: result.scheduledRunId };
    };
    return {
        /**
         * Schedule a wonderful function to run at a specific time.
         * @param options.slug - The slug of the scheduled function
         * @param options.runAt - When to execute: a Date object, a unix timestamp in ms, or an ISO 8601 string
         * @param options.params - Parameters to pass to the function
         * @param options.maxRetries - Max retry attempts on failure (default 3, set 0 for no retries)
         * @returns An object with the scheduledRunId
         */
        schedule,
        /**
         * Run a wonderful function synchronously and return its result.
         * Blocks until the function has finished executing.
         * @param options.slug - The slug of the scheduled function
         * @param options.params - Parameters to pass to the function
         * @returns An object with `result` (the function's output) and `logs` (entries from context.logger).
         */
        run: (options) => {
            const raw = functionsFunctions.run(options.slug, JSON.stringify(options.params ?? {}));
            const parsed = JSON.parse(raw.resultJson);
            return {
                result: parsed.result ?? {},
                logs: parsed.logs ?? [],
            };
        },
        /**
         * Dispatch a wonderful function to run as soon as possible without waiting for its result.
         * Fire-and-forget: thin wrapper over `schedule` that sets `runAt` to now.
         * @param options.slug - The slug of the scheduled function
         * @param options.params - Parameters to pass to the function
         * @param options.maxRetries - Max retry attempts on failure (default 3, set 0 for no retries)
         * @returns An object with the scheduledRunId
         */
        dispatch: (options) => schedule({ ...options, runAt: new Date() }),
    };
}
function createTables() {
    const toWireFilter = (f) => {
        // `in` filters carry an array; the WIT contract is a single string per
        // filter, so we JSON-encode arrays here and let the host decode them.
        // `is_null` / `is_not_null` ignore value.
        if (f.operator === "in") {
            return { column: f.column, operator: "in", value: JSON.stringify(f.value ?? []) };
        }
        if (f.operator === "is_null" || f.operator === "is_not_null") {
            return { column: f.column, operator: f.operator, value: "" };
        }
        return { column: f.column, operator: f.operator, value: f.value == null ? "" : String(f.value) };
    };
    const toWireOrderBy = (orderBy) => {
        if (!orderBy || orderBy.length === 0)
            return [];
        return orderBy.map((o) => ({ column: o.column, descending: o.direction === "desc" }));
    };
    return {
        /**
         * Query rows from a custom table by name.
         * @param tableName - The name of the custom table
         * @param limit - Maximum number of rows to return (default 100)
         * @param offset - Number of rows to skip (default 0)
         * @param orderBy - Optional ordering, applied left-to-right. When omitted, falls back to `created_at` descending.
         * @returns An object with `rows` (array of {id, data}) and `total` count
         */
        query: async (tableName, limit = 100, offset = 0, orderBy) => {
            const result = tablesFunctions.query(tableName, limit, offset, toWireOrderBy(orderBy));
            return {
                rows: result.rows.map((r) => ({
                    id: r.id,
                    data: JSON.parse(r.data),
                })),
                total: Number(result.total),
            };
        },
        /**
         * Insert a new row into a custom table.
         * @param tableName - The name of the custom table
         * @param data - The row data as a key-value object
         * @returns The inserted row with its id and data
         */
        insert: async (tableName, data) => {
            const result = tablesFunctions.insert(tableName, JSON.stringify(data));
            return {
                id: result.id,
                data: JSON.parse(result.data),
            };
        },
        /**
         * Update an existing row in a custom table.
         * @param tableName - The name of the custom table
         * @param rowId - The ID of the row to update
         * @param data - The fields to update as a key-value object
         * @returns The updated row with its id and data
         */
        update: async (tableName, rowId, data) => {
            const result = tablesFunctions.update(tableName, rowId, JSON.stringify(data));
            return {
                id: result.id,
                data: JSON.parse(result.data),
            };
        },
        /**
         * Get a single row by its primary key value.
         * @param tableName - The name of the custom table
         * @param rowId - The primary key value of the row
         * @returns The row with its id and data
         */
        getRow: async (tableName, rowId) => {
            const result = tablesFunctions.getRow(tableName, rowId);
            return {
                id: result.id,
                data: JSON.parse(result.data),
            };
        },
        /**
         * Look up multiple rows by primary key in a single round-trip.
         * IDs that don't match any row are silently skipped — the returned
         * array contains only rows that exist. Order is implementation-defined.
         * @param tableName - The name of the custom table
         * @param rowIds - Array of primary key values
         * @returns Array of {id, data} for rows that were found
         */
        getRows: async (tableName, rowIds) => {
            const result = tablesFunctions.getRows(tableName, rowIds);
            return result.map((r) => ({
                id: r.id,
                data: JSON.parse(r.data),
            }));
        },
        /**
         * Filter rows with column-level conditions.
         *
         * Supported operators:
         * - `eq`, `neq` — equality / inequality
         * - `starts_with`, `ends_with`, `contains` — case-insensitive string match
         * - `gt`, `gte`, `lt`, `lte` — numeric / date range
         * - `in` — value must be an array (e.g. `{ column: "status", operator: "in", value: ["open", "pending"] }`)
         * - `is_null`, `is_not_null` — null check; value is ignored
         *
         * @param tableName - The name of the custom table
         * @param filters - Array of {column, operator, value} conditions, AND-joined
         * @param limit - Maximum number of rows to return (default 100)
         * @param offset - Number of rows to skip (default 0)
         * @param orderBy - Optional ordering, applied left-to-right. When omitted, falls back to `created_at` descending.
         * @returns An object with `rows` (array of {id, data}) and `total` count
         */
        filter: async (tableName, filters, limit = 100, offset = 0, orderBy) => {
            const wireFilters = filters.map(toWireFilter);
            const result = tablesFunctions.filter(tableName, wireFilters, limit, offset, toWireOrderBy(orderBy));
            return {
                rows: result.rows.map((r) => ({
                    id: r.id,
                    data: JSON.parse(r.data),
                })),
                total: Number(result.total),
            };
        },
        /**
         * Count rows matching the filters without fetching row data. Useful
         * for cheap existence checks ("does this row exist?") and dashboards
         * ("how many open tickets?") without paying to materialize and parse
         * full rows.
         *
         * Note: the host returns an `s64` which is forced to a JS `number`,
         * losing precision above 2^53 (~9 quadrillion). Not a concern for any
         * realistic custom table, but mentioned for completeness.
         *
         * @param tableName - The name of the custom table
         * @param filters - Optional filters using the same operators as `filter`
         * @returns The matching row count
         */
        count: async (tableName, filters = []) => {
            const wireFilters = filters.map(toWireFilter);
            const result = tablesFunctions.count(tableName, wireFilters);
            return Number(result);
        },
        /**
         * List the distinct non-null values of a single column, optionally
         * narrowed by filters. Values come back in their native column type:
         * strings stay strings, numeric columns return numbers, boolean
         * columns return booleans. Date columns return YYYY-MM-DD strings —
         * the exact format `filter([{operator:"in", value:[…]}])` accepts on
         * the way back in, so the round-trip pattern works without any
         * format-massaging on the caller side.
         *
         * Supports the same `limit` / `offset` / `orderBy` paging primitives
         * as `query` and `filter` so high-cardinality columns can be paged
         * through deterministically. `limit` is bounded server-side at 1000;
         * passing more is a validation error. When `orderBy` is omitted the
         * server defaults to `<column>` ASC.
         *
         * @param tableName - The name of the custom table
         * @param column - The column to project
         * @param filters - Optional filters using the same operators as `filter`
         * @param limit - Maximum number of distinct values to return (default 1000, max 1000)
         * @param offset - Number of values to skip (default 0)
         * @param orderBy - Optional ordering applied left-to-right. Defaults to `column` ASC when omitted.
         * @returns Array of distinct values typed by the underlying column
         */
        distinct: async (tableName, column, filters = [], limit = 1000, offset = 0, orderBy) => {
            const wireFilters = filters.map(toWireFilter);
            const result = tablesFunctions.distinct(tableName, column, wireFilters, limit, offset, toWireOrderBy(orderBy));
            // Each wire value is a JSON-encoded scalar so the SDK can return
            // the native type. Skip nulls (the server already filters NULL
            // at the SQL level, but be defensive — parse-and-filter is cheap).
            const out = [];
            for (const raw of result) {
                let parsed;
                try {
                    parsed = JSON.parse(raw);
                }
                catch {
                    // Malformed wire value — fall back to the raw string so we
                    // don't drop user data.
                    parsed = raw;
                }
                if (parsed === null || parsed === undefined)
                    continue;
                out.push(parsed);
            }
            return out;
        },
        /**
         * Delete a row from a custom table.
         * @param tableName - The name of the custom table
         * @param rowId - The ID of the row to delete
         */
        deleteRow: async (tableName, rowId) => {
            tablesFunctions.deleteRow(tableName, rowId);
        },
        /**
         * Semantic search against a single vector column.
         * @param tableName - The name of the custom table
         * @param query - Natural language query string
         * @param column - The vector column to search against (required)
         * @param opts - Optional search settings. metric is "cosine" (default), "l2", or "inner_product".
         * @returns An object with `results` array of {id, data, score}
         */
        semanticSearch: async (tableName, query, column, opts = {}) => {
            const result = tablesFunctions.semanticSearch(tableName, query, column, opts.metric ?? "", opts.topK ?? 5, opts.minScore ?? 0);
            return {
                results: result.results.map((r) => ({
                    id: r.rowId,
                    data: JSON.parse(r.data),
                    score: r.score,
                })),
            };
        },
        /**
         * Fuzzy lexical search on a string column. Use this for typo-tolerant
         * matching, substring search, and short-string similarity — pick
         * `semanticSearch` for synonyms / meaning instead.
         * @param tableName - The name of the custom table
         * @param query - The string to match against. For `levenshtein`, must be <= 255 bytes
         *   (Postgres fuzzystrmatch limit); longer queries are rejected.
         * @param column - The string column to query
         * @param opts - algorithm: "trigram" (default — pg_trgm similarity, any length) or
         *   "levenshtein" (edit distance, capped at 255 bytes per value).
         *   topK defaults to 5, capped at 100.
         *   threshold semantics depend on algorithm:
         *   - trigram: min similarity in 0..1 (default 0.3).
         *   - levenshtein: max edit distance, integer >= 1 (default 3). Fractional
         *     thresholds < 1 are rejected; pass similarity-style values to algorithm='trigram'.
         * @returns An object with `results` array of {id, data, score} (score in [0, 1], higher is better, regardless of algorithm).
         *   Note: with `levenshtein`, rows whose column value exceeds 255 bytes are skipped — a long row can't
         *   be close in edit distance to a short query anyway.
         */
        fuzzySearch: async (tableName, query, column, opts = {}) => {
            const result = tablesFunctions.fuzzySearch(tableName, query, column, opts.algorithm ?? "trigram", opts.topK ?? 5, opts.threshold ?? 0);
            return {
                results: result.results.map((r) => ({
                    id: r.rowId,
                    data: JSON.parse(r.data),
                    score: r.score,
                })),
            };
        },
        /**
         * Atomically increase a numeric column by the given amount.
         * @param tableName - The name of the custom table
         * @param rowId - The ID of the row
         * @param column - The numeric column to increase
         * @param amount - The amount to add (must be positive)
         * @returns The updated row with its id and data
         */
        atomicIncrease: async (tableName, rowId, column, amount) => {
            const result = tablesFunctions.atomicIncrease(tableName, rowId, column, amount);
            return {
                id: result.id,
                data: JSON.parse(result.data),
            };
        },
        /**
         * Atomically decrease a numeric column by the given amount.
         * @param tableName - The name of the custom table
         * @param rowId - The ID of the row
         * @param column - The numeric column to decrease
         * @param amount - The amount to subtract (must be positive)
         * @returns The updated row with its id and data
         */
        atomicDecrease: async (tableName, rowId, column, amount) => {
            const result = tablesFunctions.atomicIncrease(tableName, rowId, column, -amount);
            return {
                id: result.id,
                data: JSON.parse(result.data),
            };
        },
        /**
         * Aggregate rows in a custom table.
         * @param tableName - The name of the custom table
         * @param opts.aggregation - One of "count" | "sum" | "avg" | "min" | "max"
         * @param opts.column - Required for any aggregation other than "count"
         * @param opts.groupBy - Column name to group by; omit for a scalar aggregation that returns a single entry with key "total"
         * @param opts.filters - Optional column-level filters using the same operators as `filter` (eq, neq, starts_with, ends_with, contains, gt, gte, lt, lte, in). `is_null` / `is_not_null` are not supported here.
         * @returns A `{ groupKey: number }` map. For grouped queries, keys are sorted by value DESC by JS insertion order. For scalar queries, the single key is "total".
         */
        aggregate: async (tableName, opts) => {
            // Fail fast on `is_null` / `is_not_null` here — server-side DQL
            // rejects them too, but catching client-side avoids the network
            // round-trip and gives a clearer call-site error.
            for (const f of opts.filters ?? []) {
                if (f.operator === "is_null" || f.operator === "is_not_null") {
                    throw new Error(`operator '${f.operator}' is not supported in aggregate(); use filter() instead`);
                }
            }
            const wireFilters = (opts.filters ?? []).map(toWireFilter);
            const result = tablesFunctions.aggregate(tableName, opts.aggregation, opts.column ?? "", opts.groupBy ?? "", wireFilters);
            // Use a null-prototype object so a tenant-controlled group key like
            // "__proto__" or "constructor" can't pollute the prototype chain.
            const out = Object.create(null);
            for (const b of result.buckets) {
                out[b.key] = b.value;
            }
            return out;
        },
    };
}
function createCustomStorage() {
    return {
        /**
             * Upload a file to custom storage (max 10 MB for direct upload).
         * @param key - The storage key (path) for the file, e.g. "invoices/q1.pdf"
         * @param data - Raw file bytes
         * @param contentType - MIME type, e.g. "application/pdf"
         */
        upload: async (key, data, contentType = "application/octet-stream") => {
            const result = customStorageFunctions.upload(key, data, contentType);
            return {
                key: result.key,
                contentType: result.contentType,
                size: Number(result.size),
            };
        },
        /**
             * Download a file from custom storage.
         * @param key - The storage key of the file
         */
        download: async (key) => {
            const result = customStorageFunctions.download(key);
            return {
                data: new Uint8Array(result.data),
                contentType: result.contentType,
            };
        },
        /**
             * Delete a file from custom storage.
         * @param key - The storage key of the file
         */
        delete: async (key) => {
            customStorageFunctions.delete(key);
        },
        /**
         * Get metadata for a stored file without downloading its content.
         * @param key - The storage key of the file
         */
        getMetadata: async (key) => {
            const result = customStorageFunctions.getMetadata(key);
            return {
                key: result.key,
                contentType: result.contentType,
                size: Number(result.size),
            };
        },
        /**
         * Check whether a file exists in storage.
         * @param key - The storage key to check
         */
        exists: async (key) => {
            return customStorageFunctions.exists(key);
        },
    };
}
function parseToolCallResult(name, result) {
    // Check if tool threw an error
    if (result.error_message) {
        throw new Error(`Called tool ${name} failed: ${result.error_message}`);
    }
    // If the tool succeeded and it was programmable then return the output
    if (result.Response) {
        return result.Response.result;
    }
    // If the tool succeeded and it was RAG then return the answer
    if (result.answer) {
        return result.answer;
    }
    return result;
}
const originalFetch = globalThis.fetch;
function customFetch(input, init) {
    const { secretName, verify, hostOverride, ...rest } = init ?? {};
    // Convert Headers object to plain object if needed
    const baseHeaders = rest.headers instanceof Headers
        ? Object.fromEntries(rest.headers.entries())
        : (rest.headers || {});
    let hostOverrideValue = hostOverride || "";
    const processedHeaders = {};
    for (const [key, value] of Object.entries(baseHeaders)) {
        if (key.toLowerCase() === "host") {
            hostOverrideValue = value;
        }
        else {
            processedHeaders[key] = value;
        }
    }
    const request = new Request(input, {
        ...rest,
        headers: {
            ...processedHeaders,
            "x-wonderful-secret-name": secretName || "",
            "x-wonderful-verify-ssl": verify === false ? "false" : "true",
            "x-wonderful-host-override": hostOverrideValue,
        },
    });
    return originalFetch(request);
}
globalThis.fetch = customFetch;
function createBaseContextLibrary() {
    const telephony = createTelephony();
    return {
        kv: createKv(),
        secrets: createSecrets(),
        globals: createGlobals(),
        tables: createTables(),
        storage: createCustomStorage(),
        telephony: { sendSms: telephony.sendSms },
        email: createEmail(),
        functions: createWonderfulFunctions(),
    };
}
function createToolContextLibrary() {
    return {
        ...createBaseContextLibrary(),
        telephony: createTelephony(),
        agent: createAgent(),
        email: createEmail(),
        outbound: createOutbound(),
        session: createSession(),
        attachments: createAttachments(),
        campaign: createCampaign(),
        tools: createTools(),
        a2a: createA2A(),
        transcriber: createTranscriber(),
    };
}
//# sourceMappingURL=context.js.map