"use strict";
/* eslint-disable */
// import path from 'path'
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = void 0;
exports.createToolTester = createToolTester;
exports.mockContext = mockContext;
// Use runtime require to load the root native binding regardless of dist location
// Types come from the generated index.d.ts at the project root.
// eslint-disable-next-line @typescript-eslint/no-var-requires
// const native = require(path.join(__dirname, './index.js'))
const native = require('../index.js');
exports.default = native;
function serializeMatch(match) {
    if (!match)
        return { kind: 'any' };
    if (typeof match === 'string') {
        return { kind: 'string', value: match };
    }
    if (match instanceof RegExp) {
        return { kind: 'regex', value: match.source, flags: match.flags };
    }
    if (typeof match === 'object') {
        const url = match.url;
        const serializedUrl = typeof url === 'string'
            ? { kind: 'string', value: url }
            : url instanceof RegExp
                ? { kind: 'regex', value: url.source, flags: url.flags }
                : { kind: 'any' };
        return {
            kind: 'object',
            url: serializedUrl,
            method: match.method,
        };
    }
    return { kind: 'any' };
}
function ensureSerializableResponse(response) {
    if (!response)
        return;
    const maybeResponse = response;
    const looksLikeResponse = typeof maybeResponse === 'object' &&
        maybeResponse !== null &&
        typeof maybeResponse.arrayBuffer === 'function' &&
        typeof maybeResponse.text === 'function';
    if (looksLikeResponse) {
        throw new Error('Response instances are not supported in mockFetch responses. Return a plain object instead.');
    }
}
function serializeFetchHandler(handler, once) {
    if (typeof handler === 'function') {
        return {
            match: { kind: 'any' },
            handler: handler.toString(),
            once,
        };
    }
    if ('handler' in handler) {
        const { match, handler: fn, delayMs } = handler;
        return {
            match: serializeMatch(match),
            handler: fn.toString(),
            delayMs,
            once,
        };
    }
    if ('response' in handler) {
        ensureSerializableResponse(handler.response);
        const { match, response, delayMs } = handler;
        return {
            match: serializeMatch(match),
            response,
            delayMs,
            once,
        };
    }
    return {
        match: { kind: 'any' },
        once,
    };
}
function attachFetchMocks(tester) {
    return tester;
}
async function createToolTester(config) {
    const tester = await native.createToolTester(config);
    return attachFetchMocks(tester);
}
function mockContext(context) {
    const defaultContext = {
        user: {
            id: 'test-user',
            email: 'test@example.com',
            name: 'Test User',
        },
        tenant: {
            id: 'test-tenant',
            name: 'Test Tenant',
        },
        functions: {},
        environment: {},
    };
    return {
        ...defaultContext,
        ...context,
        user: { ...defaultContext.user, ...context?.user },
        tenant: { ...defaultContext.tenant, ...context?.tenant },
        functions: { ...defaultContext.functions, ...context?.functions },
        environment: { ...defaultContext.environment, ...context?.environment },
    };
}
// Augment the native prototype so mock helpers are always available.
const proto = native?.ToolTester?.prototype;
if (proto && typeof proto.mockFetch !== 'function') {
    const addHandler = async function (handler, once) {
        const serialized = serializeFetchHandler(handler, once);
        if (once) {
            await this.__mockFetchOnceInternal?.(serialized);
        }
        else {
            await this.__mockFetchInternal?.(serialized);
        }
    };
    proto.mockFetch = async function (handler) {
        return addHandler.call(this, handler, false);
    };
    proto.mockFetchOnce = async function (handler) {
        return addHandler.call(this, handler, true);
    };
}
