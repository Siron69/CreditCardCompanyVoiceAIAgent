/**
 * Wrapped Tool Script
 *
 * Auto-generated by the Wonderful CLI to execute a tool.
 * This script wraps a tool created with w.tool() and exposes its handler as main().
 */

import tool from "/home/simone/CreditCardCompanyVoiceAIAgent/src/skills/fraud-disputes/tools/report-suspicious-transaction.ts";

async function main(ctx: any, params: any): Promise<any> {
	// Extract the params from the context data (CLI passes params in ctx.data)
	const toolParams = ctx?.data ?? params ?? {};

	// Call the tool's handler
	return await tool.handler(ctx, toolParams);
}

// Make sure the main function is not being tree shaken by the bundler
globalThis.main = main;
